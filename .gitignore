# See https://help.github.com/articles/ignoring-files/ for more about ignoring files.

# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage
/build


# misc
.DS_Store
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

npm-debug.log*
yarn-debug.log*
yarn-error.log*

#user generated content
# profile.json is committed so the GitHub Pages build (which doesn't run the
# token-gated fetch) ships the pinned-repo data. Refresh via `npm start` then commit.
public/blogs.json

# vite
dist
*.local
